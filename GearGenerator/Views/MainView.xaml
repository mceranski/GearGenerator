<Window x:Class="GearGenerator.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:gearGenerator="clr-namespace:GearGenerator"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="Gear Generator" Height="700" Width="700">
    <Grid Margin="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <ItemsControl ItemsSource="{Binding Gears}" AlternationCount="1">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="*" MinWidth="50"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="5"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="5"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="5"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.ColumnSpan="3" Padding="2" Background="LightGray" FontWeight="DemiBold">
                                    <Run Text="Gear #"/>
                                    <Run Text="{Binding AlternationIndex, RelativeSource={RelativeSource AncestorType=ItemsControl}, Mode=OneWay}"/>
                                </TextBlock>

                                <Label Grid.Row="2" Content="Number of Teeth"/>
                                <TextBox Grid.Row="2" Grid.Column="2" Text="{Binding NumberOfTeeth, Mode=TwoWay}"/>

                                <Label Grid.Row="4" Content="Pitch Diameter"/>
                                <TextBox Grid.Row="4" Grid.Column="2" Text="{Binding PitchDiameter, Mode=TwoWay}"/>

                                <Label Grid.Row="6" Content="Pressure Angle"/>
                                <TextBox Grid.Row="6" Grid.Column="2" Text="{Binding PressureAngle, Mode=TwoWay}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <Grid Margin="0,50,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <CheckBox IsChecked="{Binding ShowGrid, UpdateSourceTrigger=PropertyChanged}">
                        <TextBlock Text="Show Grid"/>
                    </CheckBox>

                    <CheckBox Grid.Row="2" IsChecked="{Binding ShowGuidelines, UpdateSourceTrigger=PropertyChanged}">
                        <TextBlock Text="Show Guidelines"/>
                    </CheckBox>

                    <CheckBox Grid.Row="4" IsChecked="{Binding UseAnimation, UpdateSourceTrigger=PropertyChanged}">
                        <TextBlock Text="Animate"/>
                    </CheckBox>
                </Grid>
            </StackPanel>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ScrollViewer 
                        HorizontalScrollBarVisibility="Auto"
                        VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Gears}">
                        

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Path Stroke="LightBlue" ToolTip="Outside">
                                        <Path.Data>
                                            <EllipseGeometry RadiusX="{Binding OutsideRadius}" RadiusY="{Binding OutsideRadius}" Center="{Binding CenterPoint}"/>
                                        </Path.Data>
                                    </Path>
                                    <Path Stroke="LightGreen" ToolTip="Pitch">
                                        <Path.Data>
                                            <EllipseGeometry RadiusX="{Binding PitchRadius}" RadiusY="{Binding PitchRadius}" Center="{Binding CenterPoint}"/>
                                        </Path.Data>
                                    </Path>
                                    <Path Stroke="LightCoral" ToolTip="Base">
                                        <Path.Data>
                                            <EllipseGeometry RadiusX="{Binding BaseRadius}" RadiusY="{Binding BaseRadius}"  Center="{Binding CenterPoint}"/>
                                        </Path.Data>
                                    </Path>
                                    <Path Stroke="LightGray" ToolTip="Root">
                                        <Path.Data>
                                            <EllipseGeometry RadiusX="{Binding RootRadius}" RadiusY="{Binding RootRadius}" Center="{Binding CenterPoint}"/>
                                        </Path.Data>
                                    </Path>
                                    <Path Data="{Binding GearGeometry}" Stroke="Black" StrokeThickness="1">
                                        <Path.RenderTransform>
                                            <RotateTransform x:Name="MyAnimatedTransform" CenterY="{Binding CenterY}" CenterX="{Binding CenterX}" Angle="0"/>
                                        </Path.RenderTransform>
                                        <Path.Style>
                                            <Style>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DataContext.UseAnimation, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" Value="True">
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard TargetProperty="RenderTransform.Angle">
                                                                    <DoubleAnimation From="0" To="360" RepeatBehavior="Forever" Duration="0:0:15" />
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                        <DataTrigger.ExitActions>
                                                            <BeginStoryboard>
                                                                <Storyboard TargetProperty="RenderTransform.Angle">
                                                                    <DoubleAnimation From="0" To="360" RepeatBehavior="Forever" Duration="0:0:0" />
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.ExitActions>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Path.Style>
                                    </Path>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <gearGenerator:DrawingCanvas x:Name="ZoomCanvas" Background="Transparent"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>

                <DockPanel Grid.Row="2">
                    <TextBlock Text="{Binding ElementName=Slider, Path=Value, StringFormat={}{0:P0}}" DockPanel.Dock="Right"/>

                    <Slider x:Name="Slider"
                                Maximum="5" 
                                Minimum=".25" 
                                Value="1"
                                Foreground="Black"
                                IsSnapToTickEnabled="True"
                                TickPlacement="BottomRight"
                                Ticks=".25, .5, .75, 1, 2, 3, 4, 5" />
                </DockPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
